# MEDQUEUE Dependency Installation TODO - UPDATE

## Completed Steps:
- [x] Step 0: Created TODO.md
- [ ] Step 1: Install frontend dependencies (npm install at root) - Policy set, retry pending
- [ ] Step 2: Install backend Node.js dependencies (npm install in /backend)
- [ ] Step 3: Install Python deps for ai_engine (pip install -r requirements.txt in /backend/ai_engine)
- [ ] Step 4: Install Python deps for symptom (pip install -r requirements.txt in /symptom)
- [ ] Step 5: Install Python deps for ml-service (pip install -r requirements.txt in /ml-service)

**Status:** PowerShell execution policy updated for npm. Tool issues with parallel npm, proceeding to pip installs first. Node retries next. If npm errors persist, run manually: Set-ExecutionPolicy done, now `npm install` should work.


